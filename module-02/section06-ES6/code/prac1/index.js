const productName = "Laptop";
const productPrice = 1000;
const quantity = 3;

const message = `You bought ${quantity} units of ${productName} \ for a total of $${
  productPrice * quantity
}`;

console.log(message);
